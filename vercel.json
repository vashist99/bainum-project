{
  "buildCommand": "cd mockup1 && npm install && npm run build",
  "outputDirectory": "mockup1/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
