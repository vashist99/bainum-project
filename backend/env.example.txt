# MongoDB Connection
MONGO_URI=your_mongodb_connection_string

# RevAI API Configuration
# Get your API key from https://www.rev.ai/auth/signup
REVAI_API_KEY=your_revai_api_key_here

# OpenAI API Configuration (for RAG pipeline)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Email Configuration (for sending parent invitations)
# 
# Option 1: Gmail SMTP (for local development only - blocked on Render)
# For Gmail, use an App Password: https://support.google.com/accounts/answer/185833
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-password
# EMAIL_APP_PASSWORD=your-app-password
# EMAIL_PORT=587
# EMAIL_FROM_EMAIL=your-email@gmail.com
# EMAIL_FROM_NAME=Bainum Project
#
# Option 2: Brevo (recommended - works reliably on Render)
# Option 2a: Brevo API (preferred - more reliable than SMTP)
# Get API key from: https://www.brevo.com → Settings → SMTP & API → API Keys
# BREVO_API_KEY=your-brevo-api-key
# EMAIL_SERVICE=brevo
# EMAIL_FROM_EMAIL=vashistgh99@gmail.com
# EMAIL_FROM_NAME=Bainum Project
#
# Option 2b: Brevo SMTP (alternative if API doesn't work)
# Get SMTP credentials from: https://www.brevo.com → Settings → SMTP & API → SMTP
# EMAIL_SERVICE=brevo
# EMAIL_USER=your-brevo-account-email@example.com
# EMAIL_PASSWORD=your-brevo-smtp-key
# EMAIL_PORT=587
# EMAIL_FROM_EMAIL=vashistgh99@gmail.com
# EMAIL_FROM_NAME=Bainum Project
#
# Option 3: Resend API (recommended for production)
# Get API key from: https://resend.com/api-keys
# RESEND_API_KEY=re_your_api_key_here
# RESEND_FROM_EMAIL=noreply@yourdomain.com (optional, uses onboarding@resend.dev if not set)
# EMAIL_FROM_NAME=Bainum Project

# Frontend URL (for invitation links and email templates)
# This is used for generating invitation links in emails
FRONTEND_URL=http://localhost:5173

# CORS Allowed Origins (comma-separated list of frontend URLs)
# These are the URLs that are allowed to make requests to the backend API
# For production, include your Vercel frontend URL
# For development, localhost origins are automatically included
# Example: ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://another-frontend.vercel.app
# If not set, will fallback to FRONTEND_URL
ALLOWED_ORIGINS=https://bainum-frontend-prod.vercel.app,https://bainum-project-saf2.vercel.app,http://localhost:5173,http://localhost:3000

# Upstash Redis (for rate limiting)
# Get your credentials from https://console.upstash.com/
# Create a Redis database and copy the REST URL and Token
# If not configured, the app will fall back to in-memory rate limiting
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# Server Configuration
PORT=5000

# JWT Secret Key (for authentication tokens)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here

# RAG Pipeline Configuration
# Enable/disable RAG classification (set to 'true' to enable)
RAG_ENABLED=true
# Weight for RAG scores (0.0 to 1.0, default: 0.7)
RAG_WEIGHT=0.7
# Weight for keyword-based scores (0.0 to 1.0, default: 0.3)
KEYWORD_WEIGHT=0.3
# Number of similar examples to retrieve per category (default: 5)
RAG_TOP_K=5
# OpenAI embedding model (default: text-embedding-3-small)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# OpenAI classification model (default: gpt-4o-mini)
OPENAI_CLASSIFICATION_MODEL=gpt-4o-mini
