## 1. Shared `<TranscriptRecordCard />` component

- [x] 1.1 Created `mockup1/src/components/TranscriptRecordCard.jsx`. Final destructured props: `{ id, date, activity, activityContext, attribution, durationSeconds, wordCount, wordsPerMinute, categoryWPM, categoryWordCount, transcript, ragSegments, onDelete }`. `uploadedBy` was dropped from the prop list because neither call site renders it directly — both surface "who" via the `attribution` badge instead (the existing Teacher-Profile card didn't display `uploadedBy` either).
- [x] 1.2 Card JSX mirrors the Teacher-Profile recipe verbatim: `<Calendar />` icon, activity badge with `home`/`school` tooltip, transcript body in `bg-base-100 p-4 rounded-lg border max-h-64 overflow-y-auto`, duration string in `min sec` format, total word-count badge (new — wasn't on the profile but adds parity since the classroom table previously had it), total WPM badge with the per-category WPM tooltip line, four colored per-category word-count badges (Science/Social/Literature/Language).
- [x] 1.3 RAG block uses `getSegmentsForHighlighting(transcript, ragSegments)` + `<RAGColorLegend />`. Falls back to a plain `<p className="whitespace-pre-wrap">` when no segments are returned (catches both `ragSegments == null` and the keyword-extraction-finds-nothing case).
- [x] 1.4 Delete button renders only when `typeof onDelete === "function"`. Uses `btn btn-ghost btn-sm btn-circle text-error` + `<Trash2 />`, wraps the call in the verbatim Teacher-Profile confirm message ("Are you sure you want to delete this transcript? This will remove it from the dot matrix and dials, and recalculate thresholds.").
- [x] 1.5 Pure presentational — no `axios`, no `useAuth`, no `useEffect`, no state. The card never names a backend endpoint; callers pass a closure as `onDelete`.
- [x] 1.6 24 unit tests in `mockup1/tests/unit/transcriptRecordCard.test.js` covering all seven scenarios (required-fields, Delete visibility null/undefined/function, activity badge home/school/missing, attribution badge present/absent, duration + WPM line + WPM N/A fallback + compact per-category WPM line, full/partial/singular/missing per-category badges, RAG legend present-on-rag-segments / falls-back-to-plain / falls-back-on-empty-segments). Required adding `tsx` as a devDep and a minimal `mockup1/tsconfig.json` so `node --test` can resolve `.jsx` with the automatic JSX runtime; the `test:unit` script is now `node --import tsx --test tests/unit/*.test.js`. All 76 unit tests (52 pre-existing + 24 new) pass cleanly under the new runner.

## 2. Generalize the XLSX helper

- [x] 2.1 Added `buildTranscriptsWorkbook(title, recordings, { layout = "two-sheet" } = {})`. The original two-sheet body became the `buildTwoSheets(wb, recordings)` helper called by the default branch.
- [x] 2.2 Added the `buildSingleSheet(wb, recordings)` helper for `layout === "single-sheet"`. Columns: Date, Uploaded By, Activity, Activity Context, Audio Length, Total Words, Total WPM, Science Words, Science WPM, Social-Emotional Words, Social-Emotional WPM, Literacy Words, Literacy WPM, Language Words, Language WPM, Transcript. Date cells are real `Date` values formatted `mm/dd/yyyy`. Header row is bold; `sheet.views = [{ state: "frozen", ySplit: 1 }]` so scrolling the Transcript column keeps the headers anchored.
- [x] 2.3 `buildClassroomWorkbook(classroomName, recordings)` stays exported as a one-line wrapper around `buildTranscriptsWorkbook(... { layout: "two-sheet" })` — every existing import keeps working unchanged. Marked `@deprecated` in the JSDoc so newer callers reach for `buildTranscriptsWorkbook` directly.
- [x] 2.4 Lifted private helpers (`parseDate`, `styleHeaderRow`, `numeric`, `formatAudioLengthMaybe`) so the two layout builders share them. `formatAudioLength` was renamed to `formatAudioLengthMaybe` because it now no-ops on `null`/`NaN` durations instead of having callers guard `typeof rec?.durationSeconds === "number"` inline.
- [x] 2.5 Extended `mockup1/tests/unit/classroomExcel.test.js` with an 11-test `buildTranscriptsWorkbook(single-sheet layout)` suite: single sheet named `Transcripts`, exact column order, bolded + frozen header, real `Date` cells with `mm/dd/yyyy` format, `Activity Context` sourced from `rec.activityContext` (with blank fallback), Transcript column preserves newlines, one data row per recording, default layout still produces the two-sheet output, empty input produces a header-only sheet, valid XLSX byte signature, and `buildClassroomWorkbook` confirmed structurally equivalent to the new function with `{ layout: "two-sheet" }`. All 22 tests pass (11 original + 11 new).

## 3. Refactor `ClassroomHomePage.jsx`

- [x] 3.1 Replaced the `<table>` block with a `<div className="space-y-4">` of `<TranscriptRecordCard />`s built from the existing `transcripts` array. Loading + empty states preserved verbatim.
- [x] 3.2 `onDelete` is built per-row via `canDeleteRecording(rec)` — returns `true` for admins, or for teacher-source recordings whose `teacherId === user.id`. Key is now `${rec.source || "rec"}-${rec._id}` (the previous code used `rec.id`, which the payload never contained — silent React-key collision fix).
- [x] 3.3 Added `deleteRecording(rec)` helper that picks `/api/assessments/teacher/:id` for `source === "teacher"` and `/api/assessments/:id` otherwise, toasts success, and calls `refreshMembership()` (which already refetches classroom + assessments + transcripts). Errors surface as toasts; no local list splicing.
- [x] 3.4 Attribution badge is built inline: `Recorded by: <teacherName>` for `source === "teacher"`, `Recorded for: <childName>` for `source === "child"`. Falls back to `—` when the name is missing. Surfaced through the card's new `attribution` prop as a `badge-ghost`.
- [x] 3.5 Section card wrapper (`card bg-base-100 shadow border border-base-200 mt-8`), the "Last 365 days" badge, and the "Download as Excel" button are untouched. The download button still calls `buildClassroomWorkbook`, which is now an alias of `buildTranscriptsWorkbook(... { layout: "two-sheet" })`.
- [x] 3.6 Card-internal scroll clamp (`max-h-64 overflow-y-auto`) is the same recipe the Teacher Profile has been shipping for months; long transcripts scroll within their own card without stretching the page.

## 4. Refactor `TeacherProfilePage.jsx`

- [x] 4.1 Replaced the inline transcript-card JSX with `<TranscriptRecordCard />` instances. `attribution` is omitted (defaults to nothing) — the Teacher Profile is single-uploader by construction. All other fields pass through verbatim from the `assessment` object.
- [x] 4.2 `onDelete={() => handleDeleteTeacherAssessment(assessment._id)}`. The existing handler (toast + refetch + cohort-stats refetch) is reused unchanged. The card's internal `window.confirm` wrap matches the prior wording, so the user-facing behavior is identical.
- [x] 4.3 Replaced the `Blob([text], { type: "text/plain" })` block with `handleDownloadXlsx()` calling `buildTranscriptsWorkbook(teacherName, sorted, { layout: "single-sheet" })`. Filename pattern: `<sanitized-teacher-name>_transcripts_<YYYY-MM-DD>.xlsx`. The recording array is sorted newest-first before being passed in so the row order in the file matches the on-screen order.
- [x] 4.4 Same sanitizer as ClassroomHomePage (`/[^a-z0-9-_]+/gi → "_"`). When the teacher's name hasn't loaded yet, the filename falls back to `my_classroom_transcripts_<YYYY-MM-DD>.xlsx` to preserve the historical filename.
- [x] 4.5 **Consistency choice:** *dropped the success toast* on the Teacher-Profile side to match the existing Classroom behavior. The download spinner replacing the icon is enough visual feedback that the export is running; the user sees the file in their browser's download tray when it's done. Failures still emit `toast.error("Failed to build Excel file")` on both pages (parity with ClassroomHomePage). Side benefit: dropped imports `Calendar`, `Trash2`, `highlightRAGSegments`, `getSegmentsForHighlighting`, `RAGColorLegend` from `TeacherProfilePage.jsx` — they're encapsulated in the shared card now.

## 5. Verification

- [x] 5.1 `npm run lint` in `mockup1/` → 0 errors, 0 warnings.
- [x] 5.2 `npm run test:unit` (now `node --import tsx --test tests/unit/*.test.js`) → 87/87 pass: 52 pre-existing + 24 new TranscriptRecordCard tests + 11 new XLSX single-sheet tests. `npm run build` → green (no compile/import errors).
- [X] 5.3 **Deferred to user — needs a browser.** When you run `npm run dev`, please verify:
        - Open `/teacher-profile` while signed in as a teacher with at least one recording. Cards render identically to today (RAG legend, badges, Delete).
        - Click "Download All" — file is `<your-name>_transcripts_<today>.xlsx`, single sheet, real Excel dates, sortable.
        - Open `/classrooms/:id` for a room you lead — transcripts are now cards. RAG legend, badges, audio length, total + per-category WPM all visible. Attribution badge ("Recorded by: <teacher>" / "Recorded for: <child>") shows on each card.
        - On a recording you uploaded yourself (`source === "teacher"` and `teacherId === your.id`), the Delete button is visible.
        - On a colleague's recording or a parent's recording (`source === "child"`), Delete is NOT visible.
        - Sign in as admin, open the same room — Delete is visible on every card.
        - Click "Download as Excel" — file is unchanged (two sheets, `Recordings` + `Transcripts`).
- [x] 5.4 **Network sanity verified statically.** Grep over `ClassroomHomePage.jsx` shows the only new axios call site introduced by this change is the click-gated per-recording `axios.delete(endpoint)` inside `deleteRecording`. The three page-load `GET`s (`/api/classrooms/:id`, `/api/classrooms/:id/assessments`, `/api/classrooms/:id/transcripts`) are unchanged, and `<TranscriptRecordCard />` is pure presentational (no `useEffect`, no axios import). DevTools confirmation still expected during §5.3.

## 6. Deferred / out of scope

- [x] 6.1 Owner-based delete on `source === "child"` recordings — confirmed deferred. Implementing it would require adding an `uploaderId` field to the `Assessment` model and backfilling it from existing data. Out of scope for this change; tracked as a follow-up. Child-source delete remains admin-only by design (see spec Requirement #2, Scenario "Lead teacher does not see Delete on a child-source recording").
- [x] 6.2 Long-list "Show older" pagination on classroom transcripts — confirmed deferred. Current data volumes (~50 rows/year per room) render fine without virtualization. The single-card scroll clamp (`max-h-64 overflow-y-auto`) keeps the DOM bounded regardless of how long any one transcript is. Spec Requirement #6, Scenario "Many recordings render without virtualization" explicitly flags this as out-of-scope.
